import React from 'react'
import logo from './images/upttmbi.svg'

const Logo = ({className}) => {
  return (
    <img className={className} src={logo} alt="Logo de la upttmbi" />
  );
}

export default Logo
